http:
  layer: http
  match:
    http.uri:
      startswith|any:
        - "/api/jsonws/"
        - "nonexistent"
      any: true
    http.method:
      is:
        - "POST"
    tcp.hello: abcd
  any: true
  tags:
    - "http"
